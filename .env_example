# VPS SSH Wrapper - Example .env file
# Copy to .env and edit your values. Add to .gitignore!

# Required: Format must be username@hostname
login=yourusername@your-vps.example.com

# Password-based auth (fallback)
password=your-strong-password-here

# Key-based auth (recommended - comment out password if using)
# SSH_KEY_FILE=id_rsa  # Name of private key file in ./keys/ (e.g., id_rsa, id_ed25519.pem)
